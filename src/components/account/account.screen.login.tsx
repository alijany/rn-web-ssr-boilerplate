import React from 'react';
import {RootScreenProps} from '../../navigation/model';
import {Text} from 'react-native';

export const LoginScreen: React.FC<RootScreenProps<'Auth'>> = () => {
  return <Text>LoginScreen</Text>;
};
