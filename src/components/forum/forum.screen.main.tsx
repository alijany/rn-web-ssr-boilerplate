import React from 'react';
import {Text} from 'react-native';
import {BottomTabStackProps} from '../../navigation/model';

export const ForumScreen: React.FC<BottomTabStackProps<'Forum'>> = () => {
  return <Text>HomeScreen</Text>;
};
